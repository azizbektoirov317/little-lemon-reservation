import React,{useState} from 'react';function ReservationForm({onConfirm}){const[name,setName]=useState('');const handleSubmit=(e)=>{e.preventDefault();if(name.trim().length>0){onConfirm();}};return(<form onSubmit={handleSubmit}><h2>Reservation</h2><input value={name} onChange={e=>setName(e.target.value)} placeholder='Your name'/><button type='submit'>Reserve</button></form>);}export default ReservationForm;